# Variables de entorno para el BUILD del frontend (Vite)
# Este archivo SÍ puede ir en git (no contiene secretos)
# Se usa al construir: npm run build

# La API está en el mismo dominio que el frontend
# Nginx proxea /api/ → FastAPI interno
VITE_API_URL=
